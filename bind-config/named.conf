options {
    directory "/var/cache/bind";
    listen-on port 53 { 127.0.0.1; any; };
    recursion yes;
    allow-recursion { any; };
    max-cache-size 256M;
};

zone "." IN {
    type hint;
    file "/etc/bind/db.root";
};
